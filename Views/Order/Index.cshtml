@model IEnumerable<WingTruck.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<h1 class="cuscreate-txt">Anything, anywhere!</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderTypeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderType)
            </th>
            
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.OrderTypeId)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderType)
            </td>
           @* <td>
                <a asp-action="Edit" asp-route-id="@item.OrderTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderTypeId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
